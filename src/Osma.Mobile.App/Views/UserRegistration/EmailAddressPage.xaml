<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Osma.Mobile.App.Views.UserRegistration.EmailAddressPage">
      <StackLayout>
        <Label Text="{Binding FullName, StringFormat='The angle is {0} degrees'}" />
          <Label Text="We are so happy that you are here! We just need a few personal details." />
          <Label Text="What is your email address?"/>
        <Entry Text="{Binding EmailAddress}" 
                 Placeholder="Please enter your email address here"
                 PlaceholderColor="Olive"/>
        <Button 
                VerticalOptions="CenterAndExpand"
                Text="Advance"
                Command="{Binding OpenPhoneNumberPageCommand}"/>
    </StackLayout>
</ContentPage>